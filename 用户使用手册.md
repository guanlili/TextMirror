# TextMirror 用户使用手册

先审校、再人工采纳、保存草稿或版本，最后导出。AI 建议、模型共识和“任务成功”都不等于文稿正确，请同时查看覆盖状态。

## 一、登录与入口

默认开发地址为 http://localhost:3022，实际地址及账号由管理员提供。工号密码登录始终可用；飞书授权登录仅在管理员启用并配置后显示。是否允许游客及游客的长度、次数、上传限制由管理员设置。

登录后生成的审校记录可在「校对历史」继续处理；游客没有持久审阅草稿。AI 审校与润色会向所选模型服务发送文本，私有化部署不代表外部模型调用自动离线。

## 二、文本审校

1. 进入「文本校对」，粘贴原文。
2. 选择领域：自动、通用、公文或法律。文本页默认自动；特定行业规则由管理员在「审校规则」维护。
3. 选择审校模式和模型，点击「开始校对」。

| 模式 | 适用方式 |
|------|----------|
| 单模型 | 使用一个模型；可选快查、标准、深度 |
| 多模型对比 | 选择 2–4 个已启用模型，比较问题与覆盖状态；某个模型失败不代表它未发现问题 |
| 角色协作 | 规则检查、语言审校、一致性审校、争议复核共用所选模型；不是多人编辑，也不等于四个模型投票 |

| 单模型深度 | 行为 |
|------------|------|
| 快查 | 仅运行词库、一致性及格式规则，不调用 AI |
| 标准（默认） | 规则与 AI 审校，尽量关闭思考模式以降低耗时 |
| 深度 | 增加思考与二次自检；思考支持、效果和耗时依模型而异 |

快查不产生 AI Token，但仍计平台使用次数并受配额、限流约束。角色协作必须登录并具有 `proofread:text` 权限，游客开关不代表允许所有审校模式。协作模式当前限制为 8000 字，每个任务计一次应用额度；模型供应商费用按实际调用产生，与应用次数不是同一口径。

## 三、处理、保存与恢复

### 处理问题

- 「仅修改此处」按定位采纳；「全文同类」处理同类匹配项；「忽略」保留原文；「撤销」取消此前决定。
- 「一键修改全部」需确认。不同建议覆盖同一段时，应人工选择，不能同时采纳相互重叠的修改。
- 「修订预览」显示采纳后的内容；「原文」和「原始排版」不随采纳变化。
- 问题高亮按严重程度区分，不能只凭颜色判断问题类型；无法可靠定位的问题不应盲目应用。

### 保存草稿与版本

登录记录的「审阅工作区」提供：

| 操作 | 含义 |
|------|------|
| 保存草稿 | 手动保存当前采纳、忽略决定和覆盖状态 |
| 保存版本 | 保存不可变快照，名称可留空；每条记录最多 20 个版本 |
| 版本对比 | 比较原文、当前草稿或已存版本的采纳决策，不比较页面排版 |
| 恢复此版本 | 只更新当前本地草稿；还需再次保存才能持久化 |

首次审校完成后，有记录的结果会绑定审阅地址；刷新或从历史「继续审阅」可恢复，**未保存的采纳决定不会自动保存**。游客内容只在本次页面内存中，刷新或离开可能丢失。

如提示草稿已被其他页面更新，先保留自己的修改，再明确重新读取已保存草稿；系统不会静默覆盖远端版本。

### 覆盖状态与补查

| 状态 | 如何理解 |
|------|----------|
| 完整 | 本次审校范围内流程已完成，不代表原文没有错误 |
| 部分完成 | 某些分片或模型未完成，零问题也不能判断全文无误 |
| 未记录覆盖 | 老记录缺少足够信息，无法确认是否完成全文审校 |

出现失败段时，可「仅补查这一段」或「补查全部失败段」。补查发送失败段及少量上下文，**每段按一次普通审校计额度**；原结果和已采纳项保留，新增问题仍需审阅并保存。

### 历史与导出

「校对历史」支持类型、领域筛选，展示模式、覆盖状态及已采纳 / 已忽略 / 待处理数量。点击「继续审阅」不会重新调用模型；「重新校对」「再次润色」只携带原文进入对应页面，点击开始后才发起新调用。

文本页可导出「修改后全文 TXT」或「问题报告 TXT」。修订全文只包含已采纳项，报告保留问题及处理状态；下载不等于保存草稿。

## 四、文档上传审校

1. 进入「文档校对」，拖入或选择文件。
2. 选择领域和模型，点击「开始校对」，等待任务进度。
3. 检查覆盖范围，逐项采纳并保存。
4. 按需选择「导出修订文本」（TXT）、「导出问题报告」（TXT）或 Word 导出。

| 格式 | 当前边界 |
|------|----------|
| DOCX | 提取正文非空段落；表格等非正文内容可能可预览，但不在该文本提取范围内 |
| DOC | 尝试兼容解析；失败时请用 Word / WPS 另存为 DOCX |
| PDF | 最多 100 页，只提取文本，不做 OCR；扫描件需先转换为可复制文本 |
| TXT | 纯文本审校 |

文件默认上限 20MB，实际以管理员配置和接口校验为准。

**排版与导出**：来源为 DOCX 时才尝试在原文件上应用已采纳项并保留排版。跨段、换行或特殊内容无法安全回写时会拒绝 Word 导出，应改用 TXT 并人工处理。其他格式导出的 Word 是纯文本重建，不保留 PDF 或旧 DOC 的原排版。历史源文件已删除或没有可靠关联时，也应使用 TXT。

取消为任务阶段间退出，不保证立即终止正在进行的模型请求。普通文档取消或失败时，系统按实际预扣凭据幂等退还账号额度；成功任务不退，无扣费凭据的旧任务不能自动推断退款。供应商已发生的调用费用不会因此撤销。

## 五、AI 润色

进入「AI 润色」，选择风格，输入 10–5000 字文本，点击开始。单模型生成轻量、标准、深度三个版本；多模型模式比较所选模型的输出，支持流式查看与停止生成。

提供正式规范、亲和自然、通俗易懂等 10 种风格，以页面列表为准。润色是表达改写，不是事实校验；保留专有名词、数字和原意仍需人工检查。停止显示或请求不代表已发生的模型费用退还。

## 六、词库、放行词与个人信息

- **个人词库**：新建词库，添加“错误写法 → 正确写法”，在词库列表启用 / 停用。批量导入为粘贴逗号分隔内容，可先下载模板；不需在审校页额外勾选词库。
- **放行词**：添加永久或有到期时间的临时放行词，支持逐行批量导入、搜索、编辑和删除。它用于减少重复提示，不会把词改成别的内容；显式个人纠错规则优先。
- **个人中心**：修改头像、联系方式或密码；工号等受管信息联系管理员。改密后所有设备的旧登录凭证失效，需重新登录。

## 七、事实核查（按需联网）

前提：管理员在「系统设置 → 事实核查设置」启用搜索服务；用户已登录并具有 `fact-check:run` 权限。不要求先完成文本审校。

### 独立工作台

1. 进入「事实核查」(`/fact-check`)，粘贴文本、上传文档，或导入自己的审校记录。上传仅提取文字，不执行审校；材料最多 20000 个 Unicode 字符，超长不会自动截断。
2. 选择「开放网络检索」或「指定可信信源」；后者需选择管理员已启用的信源。
3. 确认材料可以发送至外部模型与检索服务，选择「开始事实核查」直接执行，或「先确认事实项」。
4. 若先确认，待提取完成后勾选事实项并按需调整陈述，再点击「确认并核查」。调整陈述不改变原文快照，确认前尚未完成证据核查。
5. 查看原文、事实清单、检索过程与证据，区分“已尝试核查”“有正文引用”和实际结论。

核查结束后，可针对某一事实「发起单条深查」，按需补充最多 3 个证据链接并再次确认外发。它另建一份报告、另计核查次数，不刷新其他事实，也不是文本审校的“深度”档。

具有 `fact-check:review` 权限时，可为已结束任务中已检查的事实保存“认可结论 / 提出异议 / 仍待核实”；提出异议需说明依据，人工意见不覆盖机器结论。具有 `fact-check:export` 权限时，可下载 HTML 打印版或 JSON，包含核查快照和人工复核；不是直接生成 PDF。

### 从审阅页进入

已有审校记录也可使用原来的审阅面板：

1. 在审阅页「展开事实核查」。展开只加载配置和历史，不会自动搜索。
2. 选择「联网搜索」或「可信信源」；后者需要选择管理员已启用的信源。
3. 确认“内容可对外检索；涉密材料请勿使用”，再点击「开始事实核查」。
4. 展开事实条目，查看初始轮、反证 / 更正轮状态，以及证据原文、主体 / 事件时间 / 统计范围与单位核对、引文上下文。

从审校记录导入时，核查对象是该记录的**原始文本**，不是当前采纳后的草稿；独立输入则以提交的文本或文档提取结果保存不可变快照。结果为证据支持、证据反驳、证据不足或证据冲突，只供人工参考，不参与一键采纳。

注意：

- 两种模式都会联网并向外部服务发送待核查内容。可信信源仅限制可采纳证据的域名和路径，不能保证模型只访问这些站点。
- 搜索摘要不直接作为证据；引文需能在实际抓取的正文中逐字匹配。主体、事件时间和统计口径是模型评估，程序并未独立证明其语义正确。
- “反证轮完成”不代表找到了反证；找不到反证或证据不足也不代表原文正确。搜索 / 抓取不完整时应保守处理。
- SHA-256 指纹对应送给模型的归一化正文前缀，不是完整网页或原始 HTML。旧报告缺少字段会显示“未记录”。
- 每次只覆盖预算内的事实，不保证发现全文所有事实。核查每日次数包含失败和取消任务；次数与事实数上限以面板为准，搜索和模型可能产生额外费用。
- 收起或离开页面不取消服务端任务；独立工作台提供可搜索、筛选和分页的核查记录，原审阅面板显示该记录最近 20 次报告。需要终止时点击「取消核查」，当前外部请求可能仍在结束中。
- 原文、证据和人工复核默认从任务创建起保留 90 天，可由管理员配置；到期后由后台分批清理符合状态条件的记录。工作台也可主动「清理材料与证据」，运行中须先取消。两者均保留请求计数，不返还核查次数；重要报告请提前导出，上传文件另按文档保留规则管理。

## 八、质量反馈与评测

### 用户反馈

登录生成的记录支持「上报漏检」：选中原文中的具体片段，填写问题类型及可选建议。忽略建议时也可补充原因。提交反馈不改变采纳决定，且不代表模型已被判定出错或已经修复。

### 管理员审核与验证

在「管理后台 → 全局词库 → 质量反馈」：

1. 审核反馈，制作脱敏样例，确认目标位置、领域及“应报告 / 不应报告”。
2. 对应报告的目标，按需填写**认可替换**和**禁止替换**：填写精确替换文本而非解释，两组不能重叠；空文本条目表示删除。
3. 明确确认后纳入评测。仅有禁止项时，避开它仍不能证明建议正确；两组均空只评是否检出。
4. 选择最多 10 条已确认样例和 1–4 个模型，确认外部调用及费用后运行。分别查看检测指标与建议指标；坏建议不等于漏检，“未评估”不是通过。
5. 下载 JSON 报告留存；当前评测报告不在服务端持久化。纳入样例和运行评测都不会自动修改模型或规则。

「优化建议」是另一个入口：按用户接受 / 忽略事件聚合词库候选，经管理员确认后加入全局词库，不等于质量评测结论。

## 九、开放 API

### 密钥与调用

登录后在「API 密钥」创建密钥；明文只展示一次，及时保存，不要放入前端代码或公开仓库。默认最多 5 个有效密钥，可设置配额、过期时间并随时吊销；配置值以当前部署为准。

以下示例在同一终端执行，将密钥替换为自己的值；远程集成应使用部署方提供的 HTTPS 地址。

```bash
export TEXTMIRROR_BASE=http://localhost:3022
export TEXTMIRROR_API_KEY='替换为自己的 tm_ 密钥'

curl "$TEXTMIRROR_BASE/api/v1/open/proofread" \
  -H "Authorization: Bearer $TEXTMIRROR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"text":"这是一段需要审校的文本。","domain":"general","depth":"standard"}'
```

文本接口 `domain` 支持 `auto/general/official/legal`，默认 `general`；`depth` 支持 `quick/standard/deep`，默认 `standard`。`check_types` 已废弃，传入不限制审校类型。不要把网页文本页默认“自动”当作 API 默认值。

| 方法 | 路径（统一前缀 `/api/v1/open`） | 用途 |
|------|--------------------------------|------|
| POST | `/proofread` | 文本审校 |
| POST | `/proofread/compare` | 2–4 模型对比 |
| GET | `/models` | 获取可用模型及配置 ID |
| POST | `/documents` | 异步上传文档 |
| GET | `/jobs/{job_id}` | 轮询任务结果 |
| GET | `/usage?days=7` | 近 1–90 日应用调用统计 |
| POST | `/polish` | 三版本润色 |
| POST | `/polish/stream` | SSE 流式润色 |

多模型先调用 `/models`，再把返回的真实 ID 填入 `config_ids`，不要照抄其他环境的 ID。对比按成功模型数计应用额度；历史问题数是合并去重数，不是各模型数简单相加。`success=true` 也可能是部分成功，还需检查 `complete` 和覆盖字段。开放对比与文档接口不提供 `depth` 参数。

### 异步文档

```bash
export TEXTMIRROR_DOCUMENT_REQUEST_ID="$(python3 -c 'import uuid; print(uuid.uuid4())')"
curl "$TEXTMIRROR_BASE/api/v1/open/documents" \
  -H "Authorization: Bearer $TEXTMIRROR_API_KEY" \
  -H "Idempotency-Key: $TEXTMIRROR_DOCUMENT_REQUEST_ID" \
  -F "file=@报告.docx" -F "domain=official"

# 将返回的 job_id 填入下面路径；建议每 2–5 秒查询一次
curl "$TEXTMIRROR_BASE/api/v1/open/jobs/返回的job_id" \
  -H "Authorization: Bearer $TEXTMIRROR_API_KEY"
```

`Idempotency-Key` 可选但建议使用：每次新的逻辑提交生成一个键并保存；网络异常时只重跑上传 `curl`，复用同一个键，不重新执行生成命令。幂等范围隔离到原 API 密钥；服务端不会比较重复键对应的文件和参数，换文件、领域或模型必须换键，否则可能返回旧任务。不带键重试可能重复提交并消耗额度；幂等不代表已发生的供应商费用可撤销。

任务状态包括 `PENDING / PROGRESS / SUCCESS / FAILURE`，取消以 `FAILURE` 返回。密钥提交的任务必须使用**原密钥**查询，同账号的另一密钥也不能读取；开放 jobs 接口没有取消路由。

`corrected_download_url` 可能为空；有值时为带时效签名的自动修订文件地址，不能当作网页人工采纳稿。当前 DOCX / TXT 才尝试自动生成该文件；需要可控交付时使用网页审阅工作区。

### Webhook

在密钥页配置回调地址后，异步文档完成 / 失败会推送 `document.completed / document.failed`，另有 `webhook.test` 测试事件。签名密钥只展示一次，重新设置地址会轮换。

收到请求后，对**原始请求体字节**计算 HMAC-SHA256，与 `X-TextMirror-Signature` 的 `sha256=…` 值进行常量时间比较；校验通过后再解析内容。按事件与 `job_id` 幂等处理，并用原密钥查询完整结果。接收端返回 2xx 表示送达，失败会指数退避重试，最多 5 次。后台可查看最近投递状态和记录；非 DEBUG 环境下，回调不支持内网 / 保留地址。

### 配额与错误

- 默认单密钥每分钟 12 次；业务请求同时受账号额度与密钥日额度限制。快查也计数，查询模型 / 任务 / 用量不等同于业务调用。
- `/usage` 为基于成功记录的应用使用次数，对比按成功模型数加权；不是物理模型请求数或费用账单。
- 服务端调用失败有相应退款逻辑，但并非所有 `FAILURE` 都免费；无效配置、取消的密钥额度与账号退款口径不同。不要靠反复取消或重提推断费用。
- 开放接口错误格式为 `{"detail":{"code":"…","message":"…"}}`；参数错误通常 422，鉴权失败 401 / 403，任务不存在或无权访问 404，限流或额度不足 429，模型 / 队列不可用 503。

完整参数、响应、错误码和在线调试以 `/api/v1/open/docs` 为准；上表不替代接口契约。

## 十、管理后台与常见问题

管理员可维护用户与角色、游客策略、API 密钥、模型配置、行业规则、文档、审计日志和平台品牌。

「仪表盘 → 模型调用账本」按业务、阶段和模型展示实际请求数、已知 Token、未知用量、失败 / 未完整 / 取消、平均耗时和联网检索。重试分别计数，未返回用量显示未知而非零；仅记录账本启用后的调用，不叠加历史估算，搜索费用以供应商为准。

**为什么审校很慢或超时？** 长度、深度、模型与并发负载都会影响耗时，没有固定秒数保证。先检查覆盖状态及服务提示，再联系管理员检查模型超时、队列与供应商状态。

**看不到新功能？** 确认部署版本、权限和管理员开关；事实核查默认关闭，不能仅凭代码已更新就认为已启用或完成联网验收。

**数据保存多久？** 登录记录和上传文件由管理员管理；游客上传文件默认按保留期清理。事实核查材料默认保留 90 天（可配置），到期清理原文、证据和复核，保留请求计数；详见第七节。反馈样例可能独立保留，删除源记录不等于删除已确认评测样例。具体留存要求请联系管理员。
