# TextMirror 智能文档审校平台 · 用户使用手册

> 告别手动逐字核查，让 AI 帮你把好文字关。
> 覆盖错别字、语法、标点、敏感词、专业术语——一键完成，秒级响应。

---

## 快速登录

访问平台地址（默认 **http://localhost:3022**），支持两种方式登录：

| 方式 | 操作 |
|------|------|
| **工号密码** | 输入工号 + 密码，首次登录密码由管理员提供 |
| **飞书扫码** | 点击"飞书登录"，打开飞书 App 扫码，1秒登录 |

> 如在飞书工作台打开应用，系统会自动识别身份、免密登录。

---

## 一、AI 智能润色

> 把普通表达升级为专业用语，覆盖职场所有文体场景。

### 怎么用

1. 进入左侧菜单「**AI润色**」
2. **选择风格**（点击风格卡片即可切换）：

   | 场景 | 推荐风格 |
   |------|----------|
   | 汇报材料、正式文件 | 正式规范 / 高屋建瓴 |
   | 通知公告、内部沟通 | 亲和自然 / 落地务实 |
   | 拒绝请求、委婉表达 | 委婉缓冲 / 温和坚定 |
   | 推动工作、催促进度 | 推进行动 / 极简干练 |
   | 对外宣传、大众阅读 | 通俗易懂 / 有理有据 |

3. 粘贴原文（10～5000字），点击「**开始润色**」
4. 系统输出 **3个版本**：轻量改动 / 标准润色 / 深度重写，按需选取
5. 一键复制，或点击「换风格」换一种

---

## 二、文本在线校对

> 粘贴即校对，问题一目了然，点点鼠标全部搞定。

### 怎么用

1. 进入左侧菜单「**文本校对**」
2. 将文字粘贴到左侧编辑框
3. 选择**审校领域**（可选，默认通用）：

   | 领域 | 适合场景 |
   |------|----------|
   | 通用 | 日常文稿、邮件通知 |
   | 公文 | 正式公文、报告函件 |
   | 法律 | 合同、法规、协议 |
   | 电力 / 新能源 / 电能表 | 行业专业文档 |

4. 点击「**开始校对**」，右侧问题列表自动弹出
5. **处理问题**：
   - 鼠标悬停某条问题 → 原文对应位置高亮聚焦
   - 点击「接受」→ 单条修改生效
   - 点击「忽略」→ 跳过该条
   - 点击「**一键全部修改**」→ 预览后确认批量修改

6. 修改满意后，点击右上角「**导出**」→ 选择 TXT / DOCX / PDF

### 颜色说明

| 颜色 | 含义 |
|------|------|
| 🔴 红色 | 错别字 |
| 🟡 黄色 | 语法问题 |
| 🔵 蓝色 | 表达建议 |
| 🟠 橙色 | 敏感词 / 禁用词 |
| 🟣 紫色 | 个性化词库命中 |
| 🟢 绿色虚线 | 放行词（已设为忽略） |

---

## 三、文档上传校对

> 直接上传 Word / PDF，格式完整保留，批注改完即下载。

### 怎么用

1. 进入左侧菜单「**文档校对**」
2. 点击「上传文档」，支持格式：

   | 格式 | 限制 |
   |------|------|
   | Word（.docx） | 最大 20MB |
   | PDF（可复制文字） | 最大 20MB，≤100页 |
   | 纯文本（.txt） | 最大 20MB |

3. 上传完成后，左侧显示**文档预览**，右侧显示**问题列表**
4. 逐条处理或一键修改（操作与文本校对相同）
5. 处理完成后，点击「**下载修订文档**」获取洁净版 DOCX

> 原文的加粗、字号、表格、段落格式完整保留，下载即可直接使用。

---

## 四、个性化词库

> 录入公司专属术语，再也不会把专有名词标为"错误"。

### 适合场景

- 公司产品名称、项目代号被误判为错别字
- 行业专属表达与通用写法不同
- 特定人名、地名需要固定写法

### 怎么用

**创建词库：**
1. 进入「**词库管理**」→ 点击「新建词库」→ 输入名称（如"项目A术语库"）
2. 点击「**添加词条**」→ 填写「错误写法」→「正确写法」→ 保存

**批量导入：**
1. 下载 CSV 模板，按格式填写
2. 点击「批量导入」上传

**使用词库：**
- 校对页面右侧「词库选择」中勾选启用的词库即可
- 可同时启用多个词库

---

## 五、放行词

> 某些词系统总报错，但你确认没问题？加入放行名单，从此不再打扰。

### 怎么用

1. 进入「**放行词**」→ 点击「添加放行词」
2. 填写词语，选择类型：
   - **永久放行**：长期有效
   - **临时放行**：设置到期日期，过期自动恢复检测
3. 保存后，下次校对遇到该词自动跳过（显示绿色虚线）

---

## 六、校对历史

> 每次校对结果都有存档，随时可以找回。

进入「**校对历史**」可以：

- 按**类型**筛选：文本校对 / 文档校对 / AI润色
- 按**时间**和**领域**快速定位
- 点击某条记录 → 打开**详情抽屉** → 查看完整结果，支持重新导出

---

## 七、个人中心

点击右上角头像 → 「个人中心」：

| 功能 | 说明 |
|------|------|
| 修改头像 | 上传个人照片 |
| 修改手机号 | 更新联系方式 |
| 修改密码 | 建议定期更换 |
| 飞书绑定状态 | 查看是否已绑定飞书账号 |

> 工号、姓名、部门等信息由管理员维护，如需修改请联系管理员。

---

## 八、API 密钥（开放接口）

> 想把审校能力接进自己的脚本、CI 或工作流？用 API 密钥调用开放接口。

### 获取密钥

登录后进入左侧「**API 密钥**」页面 → 「创建密钥」：

- 密钥格式为 `tm_` 开头的长字符串，**只在创建时完整展示一次**，请立即复制保存
- 可选设置：名称（备注用途）、每日调用上限（不填则跟随账号配额）、过期时间、备注
- 每个账号最多同时持有 5 个有效密钥（已吊销的不占名额）；不再使用的密钥随时可「吊销」，立即失效
- 服务端故障导致的失败调用（503/500）不会消耗密钥当日配额

### 调用方式

接口地址：`POST /api/v1/open/proofread`，请求头携带 `Authorization: Bearer tm_你的密钥`。

curl 示例：

```bash
curl -X POST http://你的域名/api/v1/open/proofread \
  -H "Authorization: Bearer tm_xxxxxxxxxxxxxxxx" \
  -H "Content-Type: application/json" \
  -d '{"text": "这是一段需要审校的文本。", "domain": "general"}'
```

Python 示例：

```python
import requests

resp = requests.post(
    "http://你的域名/api/v1/open/proofread",
    headers={"Authorization": "Bearer tm_xxxxxxxxxxxxxxxx"},
    json={"text": "这是一段需要审校的文本。", "domain": "general"},
)
data = resp.json()
for issue in data["issues"]:
    print(issue["original"], "->", issue["suggestion"])
```

可选参数：`domain`（领域，可选值 `general`/`official`/`legal`/`power`/`new_energy`/`meter`，影响校对规则侧重，推荐按文本所属行业选择）。传入非法值会返回 422 参数错误。

> `check_types`（校对类型）已废弃：传入无效果，系统总是全量审校（该参数曾用于限定检查类型，但模型并不遵循限定，且各类型问题存在交叉，保留只会造成"选了没用"的困惑）。

### 多模型对比审校

同一文本用多个模型并发审校，返回各模型结果及交叉统计（哪些问题是所有模型的共识、哪些仅单一模型发现）。

先查可用模型列表拿到 ID：

```bash
curl http://你的域名/api/v1/open/models \
  -H "Authorization: Bearer tm_xxxxxxxxxxxxxxxx"
# 返回 {"models": [{"id": 8, "name": "火山方舟 (豆包)", ...}, ...]}
```

再用返回的 `id` 发起对比：

```bash
curl -X POST http://你的域名/api/v1/open/proofread/compare \
  -H "Authorization: Bearer tm_xxxxxxxxxxxxxxxx" \
  -H "Content-Type: application/json" \
  -d '{"text": "需要审校的文本。", "config_ids": [8, 10]}'
```

`config_ids` 为 2-4 个模型ID（来自上面的列表接口）。`id=8` 这类数值每个部署环境不同，**不要照抄文档里的示例数字**。若列表中可用模型不足 2 个，请联系管理员在后台启用更多模型配置。

> 注意：一次对比按**成功**的模型数消耗额度（2 个模型全部成功 = 2 次；某模型调用失败则该模型不计费，自动退还）。单模型调用失败不影响其他模型，对应结果项 `success=false`。

### 文档审校（异步）

上传 `.doc` / `.docx` / `.pdf` / `.txt` 文档（最大 20MB），立即返回任务ID，轮询获取结果。适合长文档集成场景：

```bash
# 1. 提交文档（multipart 表单）
curl -X POST http://你的域名/api/v1/open/documents \
  -H "Authorization: Bearer tm_xxxxxxxxxxxxxxxx" \
  -F "file=@报告.docx" \
  -F "domain=official"

# 返回 {"job_id": "...", "status_url": "/api/v1/open/jobs/..."}

# 2. 轮询任务状态（建议间隔 2-5 秒）
curl http://你的域名/api/v1/open/jobs/{job_id} \
  -H "Authorization: Bearer tm_xxxxxxxxxxxxxxxx"
```

轮询响应的 `status` 依次为 `PENDING`（排队）→ `PROGRESS`（处理中，含进度百分比）→ `SUCCESS` / `FAILURE`。`SUCCESS` 时 `result` 字段含问题列表与修订文档下载地址（`corrected_download_url`，带签名有时效）。任务失败（`FAILURE`）不消耗密钥当日配额。

仅任务提交者本人可查询任务状态，他人查询返回 404。

### 限制与错误

| 限制 | 说明 |
|------|------|
| 频率 | 单密钥每分钟最多 12 次，超出返回 `429 RATE_LIMITED` |
| 每日配额 | 跟随账号每日配额（或密钥单独设置），超出返回 `429 QUOTA_EXCEEDED / KEY_QUOTA_EXCEEDED` |

所有非 2xx 错误的 `detail` 统一为 `{"code": "...", "message": "..."}` 格式。常见错误码：

| 状态码 | code | 含义 |
|--------|------|------|
| 400 | `INVALID_CONFIG / INVALID_FILE / FILE_TOO_LARGE` | 模型配置无效 / 文件格式或内容无效 / 文件超限 |
| 401 | `UNAUTHORIZED / INVALID_API_KEY` | 未带凭证 / 密钥错误 |
| 403 | `API_KEY_REVOKED / API_KEY_EXPIRED / ACCOUNT_DISABLED` | 密钥已吊销 / 过期 / 账号被禁用 |
| 404 | `JOB_NOT_FOUND` | 任务不存在或非本人提交 |
| 422 | `VALIDATION_ERROR` | 参数错误（如 domain 传了非法值） |
| 429 | `RATE_LIMITED / KEY_QUOTA_EXCEEDED / QUOTA_EXCEEDED` | 频率超限 / 密钥配额用尽 / 账号配额用尽 |
| 503 | `MODEL_UNAVAILABLE / TASK_QUEUE_UNAVAILABLE` | 审校服务不可用 / 任务队列不可用 |

另有任务级错误码 `TASK_FAILED`（轮询响应中 `status=FAILURE` 时的 `error` 字段，非 HTTP 错误）。

完整接口文档（含参数明细与在线调试）：访问 `/api/v1/open/docs`。

---

## 常见问题

**Q：文档很长，校对会不会很慢？**
系统自动分片并发处理，通常 1万字以内 30秒内完成。

**Q：校对后能撤销修改吗？**
可以。每条修改支持单独撤销，或使用快捷键 `Ctrl+Z` 回退。

**Q：个性化词库和放行词有什么区别？**
词库是"把A改成B"；放行词是"遇到A不报错、也不改"，用途不同。

**Q：支持手机使用吗？**
支持，平台已全站适配移动端，手机浏览器直接打开即可。

**Q：我的文档内容会保存多久？**
校对记录长期保存在历史中；上传的原始文件由管理员统一管理。

---

> 有问题或建议，请联系系统管理员。
