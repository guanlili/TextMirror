# TextMirror 系统功能设计文档

本文说明模块职责和关键约定，不复制完整表结构或接口参数。操作流程见 [用户手册](用户使用手册.md)，环境和迁移见 [运维手册](系统运维操作手册.md)，字段以当前模型、Schema 和生成的 API 文档为准。

## 一、架构与模块

```text
浏览器（Vue 3 / TypeScript / Element Plus）
  → Nginx：静态资源、API 反向代理
  → FastAPI：认证、权限、业务接口
      → PostgreSQL：用户、配置、任务、审阅记录、反馈和用量
      → Redis：额度、限流、锁、Celery 消息与结果
      → Celery：文档审校、角色协作、事实核查、定时清理
      → 模型服务 / 可选搜索服务
```

后端要求 Python 3.11+，使用 SQLAlchemy 异步会话与 Alembic；前端开发和 CI 使用 Node.js 22.x（至少 22.13）。Compose 提供 PostgreSQL 16、Redis 7 和四个应用服务：前端、API、普通 worker、事实核查 worker。`fact_check.*` 任务路由到 `fact-check` 队列，由独立 worker 消费；beat 仅在普通 worker 启用。开发为桥接网络，生产为 Linux host 网络；默认入口 HTTP 3022，TLS 需另行配置。

| 模块 | 主要实现 | 职责 |
|------|----------|------|
| 审校引擎 | [proofread.py](backend/app/services/proofread.py) | 领域识别、词库与规则、分片 AI、自检、定位和覆盖 |
| 模型适配 | [llm/](backend/app/services/llm/) | OpenAI 兼容调用、供应商参数、重试与物理请求用量 |
| 多模型与协作 | [model_compare.py](backend/app/services/model_compare.py)、[collaboration.py](backend/app/services/collaboration.py) | 独立模型结果合并，或角色分工与争议复核 |
| 文档与审阅 | [document.py](backend/app/services/document.py)、[review.py](backend/app/services/review.py) | 提取文本、采纳补丁、草稿版本和安全导出 |
| 质量闭环 | [quality_feedback.py](backend/app/services/quality_feedback.py)、[quality_evaluation.py](backend/app/services/quality_evaluation.py) | 收集候选、人工审核、检测和建议评测 |
| 事实核查 | [fact_check.py](backend/app/services/fact_check.py)、[fact_check_search.py](backend/app/services/fact_check_search.py) | 搜索适配、安全抓取、正文引文和结构化判断 |
| 额度与任务 | [rate_limit.py](backend/app/core/rate_limit.py)、[task_quota.py](backend/app/core/task_quota.py)、[tasks/](backend/app/tasks/) | 原子预扣、幂等退款、任务归属与取消 |
| 前端审阅 | [useProofreadReview.ts](frontend/src/composables/useProofreadReview.ts)、[ReviewWorkspace.vue](frontend/src/components/ReviewWorkspace.vue) | 不可变原文上的采纳决策、恢复和版本对比 |

角色协作是 AI 角色工作流，不是多人实时共同编辑；多模型共识也不是正确性证明。

## 二、审校与交付数据流

```text
输入原文 / 上传提取文本
  → 加载领域、词库与模型配置
  → 确定性词库扫描、全文一致性与格式规则
  → 快查直接返回；标准 / 深度再执行分片 AI 与必要自检
  → 校验原文、定位、去重、放行词过滤、覆盖统计
  → 保存原始审校记录
  → 用户采纳 / 忽略 → 手动保存草稿或版本 → 仅采纳项导出
```

### 定位与采纳

- 原文不可变，修改稿由明确采纳的补丁生成；不能直接把模型生成的全文当作人工审阅稿。
- `start/end` 是 Unicode 码点的 0 起始半开区间 `[start, end)`，不是 JavaScript UTF-16 下标；目标必须与原文片段一致。
- 相同位置和建议可共享决定；同词出现在不同位置仍需区分。重叠采纳不能同时应用，保存时拒绝冲突。
- `review_revision` 实现乐观并发控制，旧修订号保存返回 409；版本不可变，最多 20 个，恢复只改变当前草稿。
- URL 中的 `review` 关联持久记录；首次完成和从历史继续均可恢复。草稿手动保存，游客状态不持久化。

### 覆盖与部分失败

引擎返回 `coverage`，包含已完成和失败分片。历史另外提供 `complete / partial / unknown` 汇总，兼容未记录覆盖的旧数据。多模型任一失败、角色未完成或分片失败都不能包装成全文无问题。

`success` 表示可用结果，不保证 `complete`。补查只发失败段及必要上下文，产生新的调用和应用额度，合并后仍需保存审阅状态。历史分页只投影摘要和必要 JSON 字段，不读取全部版本或为每条记录重新构建完整审阅响应。

### 文档范围与两类导出

- DOCX 当前提取正文段落，预览中的表格等内容不保证参与审校；PDF 只做文本提取，无 OCR。DOC 兼容解析失败需另存 DOCX。
- 网页审阅导出只应用采纳项；来源为 DOCX 时尝试保留排版回写，无法安全修改则拒绝。无可靠源文件关联或源文件失效不能猜测恢复排版。
- 开放异步文档的 `corrected_download_url` 是可选的自动修订产物，不等于人工采纳稿；签名下载与审阅导出是不同交付路径。

## 三、质量反馈与事实核查

### 质量反馈与评测

采纳 / 忽略事件用于词库候选；质量反馈单独收集漏检、误报、坏建议及个人偏好。用户反馈不是评测真值，管理员需脱敏、确认目标位置和预期后才能纳入。

评测分别报告：

- **检测**：目标是否被报告；不完整覆盖或调用失败不能按漏检直接计分。
- **建议**：将人工认可 / 禁止的替换与模型替换放回样例上下文精确比较。一个正确候选不能掩盖另一个坏建议。
- **未评估**：没有认可改法、没有可比较建议或只避开禁止项，都不能证明建议正确；`no_report` 样例不附带替换约束。

评测只按管理员操作运行，不自动训练模型、不修改规则；会产生真实模型费用。反馈样例持久化，评测报告由前端或 CLI 输出，不作为服务端历史报告保存。固定样例与人工反馈评测入口见 [eval.py](backend/eval/eval.py)；文章级采集与离线重评分见 [articles.py](backend/eval/articles.py)，输入和逐条结果留在私有目录。

### 事实核查

独立工作台 `/fact-check` 接受文本、上传文档提取结果或已有审校记录，不要求先做文字审校。提交时保存不可变原文快照；从记录导入的是原文而非采纳后的草稿，不改审阅决定。默认关闭，由管理员选择模型原生联网或 Tavily；失败不自动换搜索服务，用户必须确认内容可对外检索并具有 `fact-check:run` 权限。

1. 提取预算内事实并保留原文位置。可直接核查，或进入 `WAITING_CONFIRMATION`，待用户选择事实项、调整陈述后继续；调整陈述不改变原文快照。
2. 执行初始检索与反证 / 更正检索，记录每轮状态和抓取失败。
3. 安全抓取正文，执行公网地址、重定向、连接及响应限制；不直接采信搜索摘要。
4. 校验连续逐字引文；模型分别评估主体、事件时间和统计范围 / 单位。
5. 缺证据、结构无效、口径不明或技术流程不完整时保守降为证据不足，不生成可自动应用的结论。

任务结束后，单条深查创建关联 `parent_run_id` 的新报告，只重新核查所选事实，不覆盖原报告或刷新其他结论。人工复核独立存入 `fact_check_reviews`，不改机器结论；复核和导出分别受 `fact-check:review`、`fact-check:export` 权限控制，导出支持包含原文、结果及复核的 HTML / JSON。

核查材料默认从任务创建起保留 90 天（`FACT_CHECK_RETENTION_DAYS`），每小时调度分批清理符合状态条件的原文、结果和复核，保留请求计数；主动清理也不重置额度，上传源文件另有生命周期。

可信信源限制的是可采纳证据的域名与路径，不是封闭搜索。引文上下文和 SHA-256 仅对应模型可见的归一化正文前缀，不构成完整网页存档。程序验证结构、引文和保守约束，没有独立证明模型的语义判断。

## 四、持久化与调用口径

完整模型定义见 [models/](backend/app/models/)，这里仅列业务归属：

| 数据 | 用途 |
|------|------|
| `proofread_records` | 原文、原始结果、来源文件关联、应用额度权重、审阅草稿与版本 |
| `proofread_tasks`、`uploaded_documents` | 持久任务、归属与提交参数，以及上传文件生命周期 |
| `quality_feedback` | 用户候选、审核修订号和人工确认样例；源记录删除后样例可独立保留 |
| `fact_check_config`、`fact_check_runs`、`fact_check_reviews` | 搜索配置、信源、核查原文快照、状态与证据，以及独立于机器结论的人工复核 |
| `llm_usage` | 每次实际模型请求的业务、阶段、模型、结果、耗时及已知用量 |
| 用户、角色、密钥、词库和审计表 | 权限、配置、可运营规则与操作追踪 |

### 应用次数与模型用量不是一回事

- 用户额度按 Asia/Shanghai 自然日预扣；对比按成功模型数加权，协作按任务计次。快查仍计应用额度，但不发送模型请求。
- 普通文档保存原预扣 Redis key，API 和 worker 共用任务级 Lua 幂等退款。跨日不退到“今天”，成功任务不退，无凭据旧任务不猜测退款。
- 账号额度、API 密钥额度和供应商费用有不同契约；不能承诺取消或所有失败都统一退款。预扣后、任务持久化前的进程崩溃窗口尚无持久补偿账本。
- `llm_usage` 只在物理请求边界写入，重试各记一次；业务结果不能再次入账。流式保留供应商最终累计用量，不逐帧相加。
- 未返回 Token 为 NULL，不估算为零；账本写入失败不破坏主调用，但统计可能不完整。不回填旧审校估算，也不计算货币费用。
- 仪表盘应用次数来自审校记录，模型用量来自账本；`/open/usage` 是 API 应用次数统计，不是模型 Token 报表。

## 五、接口与访问控制

普通业务接口统一前缀 `/api/v1`；开发模式文档为 `/docs`。开放 API 为独立子应用，文档常开于 `/api/v1/open/docs`，使用 API 密钥或接口允许的登录凭证；不要把内部管理接口当成密钥开放接口。

| 接口组 | 主要入口（普通业务前缀下） |
|--------|----------------------------------|
| 审校与协作 | `/proofread/text`、`/proofread/compare`、`/proofread/collaborate` |
| 文档与任务 | `/document/upload`、`/document/proofread/async`、`/tasks/{task_id}` 及 stream / cancel |
| 历史与交付 | `/history/{record_id}` 及 review / versions / export |
| 质量反馈 | `/proofread/quality-feedback`、`/admin/global-dict/quality-feedback` 及 evaluate |
| 事实核查 | `/fact-check/options`、`/fact-check/history`、`/fact-check/runs` 及单次原文 / 确认 / 深查 / 复核 / 导出 / 取消 / 清理 |
| 模型账本 | `/admin/dashboard/model-usage` |

JWT 配合 RBAC；密码变更时间用于失效旧凭证。Access Token 的代码默认值为 60 分钟、Refresh 为 30 天，但环境变量可覆盖；当前配置模板仍有旧的 10080 分钟值，部署时应显式改为 60。

任务及记录按归属校验，密钥任务必须原密钥读取。用户开放 API 密钥仅存哈希，模型 / 搜索服务密钥加密保存；认证与计费等逻辑不能依赖前端隐藏按钮。不要把“Redis 故障降级”泛化为所有锁和权限检查都可放行，各业务有独立失败契约。

## 六、维护与验证

遵循 API → Service → Model 分层，前端使用 Composition API；避免在文档重复维护可从代码生成的字段清单。涉及定位、覆盖、保存冲突、退款或 Token 统计的改动需保留边界测试。

单元测试、真实服务验收和模型评测是三类不同证据：SQLite / fakeredis 测试不能证明 PostgreSQL 方言或真实 Redis Lua 行为；模拟事实报告不能替代联网核查；一次模型评测也不是稳定质量保证。测试命令与提交要求见 [贡献指南](CONTRIBUTING.md)。
