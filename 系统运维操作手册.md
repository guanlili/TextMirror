# TextMirror 系统运维操作手册

> 版本：v1.3 | 更新日期：2026-09-06
>
> v1.3：部署模式切换为「服务器端构建」；补充 CPU 架构兼容、数据库初始化与迁移、更新/回滚场景、常见问题
>
> v1.2：补充白标品牌配置、词库优化建议、审计日志自动清理、生产更新必跑数据库迁移

---

## 一、环境要求

| 项目 | 要求 |
|------|------|
| 服务器 | Ubuntu 22.04+，已安装 Docker（含 compose 插件） |
| Python | 3.10+（本地裸机开发） |
| Node.js | 22+（本地裸机开发；vite 7 要求 20.19+） |
| PostgreSQL | 生产由 compose 内置 `postgres:16-alpine`；裸机开发需 14+ |
| Redis | 生产由 compose 内置 `redis:7-alpine`；裸机开发需 7+ |
| 域名 | your-domain.com → 服务器 IP（可选） |

### 1.1 CPU 架构兼容（重要）

本项目运行环境跨架构：**本地开发机多为 Apple Silicon（arm64），生产服务器为 x86_64**。Docker 镜像与架构绑定，混用会出问题：

| 场景 | 后果 |
|------|------|
| 本地 arm64 构建的 tar 传到 x86 服务器 `docker load` | 容器无法启动（exec format error），或 qemu 模拟下诡异崩溃（如 redis 段错误） |
| 本地误拉 amd64 镜像（网络代理/镜像源缓存问题） | 在 arm 机器上一重启容器就段错误——**潜伏型故障，跑着的时候没事，重建就炸** |

**规则：**

1. **服务器端构建（推荐，零风险）**：生产 compose 已配置 `build:` 字段，在服务器上 `git pull && docker compose up -d --build`，Docker 自动按服务器架构构建。
2. **本地构建传 tar（备选离线模式）**：`build_images.bat` 已内置 `--platform linux/amd64`，本地是 arm64 也能构建出 x86 镜像（通过 qemu 模拟，构建较慢属正常）。手动构建务必带该参数：
   ```bash
   docker build --platform linux/amd64 -t textmirror-backend:latest -f backend/Dockerfile backend/
   ```
3. **基础镜像（postgres/redis 等）**：官方多架构镜像，正常 `docker pull` 会自动匹配本机架构。**镜像架构自检**（排查"一重建就崩"问题）：
   ```bash
   # 查看宿主架构
   uname -m
   # 查看已拉取镜像的架构（应与宿主一致，本地 dev 环境重点检查 redis/postgres）
   docker image inspect redis:7-alpine --format '{{.Architecture}}'
   docker image inspect postgres:16-alpine --format '{{.Architecture}}'
   # 不一致时强制重拉本机架构
   docker pull --platform linux/$(uname -m | sed 's/x86_64/amd64/;s/aarch64/arm64/') redis:7-alpine
   ```

---

## 二、本地开发

### 2.1 首次安装（Docker，推荐）

```bash
cp backend/.env.example backend/.env
docker compose -f docker-compose.dev.yml up -d --build
# 建表与 alembic 版本对齐由后端容器启动时自动完成，仅需初始化种子数据：
docker exec textmirror-dev-backend python -m app.core.seed
```

### 2.2 首次安装（裸机）

```bash
cd backend
cp .env.example .env          # 修改数据库/Redis/API Key 配置
python -m venv venv
# Windows: venv\Scripts\activate   Linux/Mac: source venv/bin/activate
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 初始化数据库（建表 + 种子数据）
python -m app.core.seed
alembic stamp head            # 标记迁移基线（仅裸机首次初始化需要，容器环境自动完成）

cd ../frontend
npm install --registry=https://registry.npmmirror.com
```

### 2.3 日常启动

```bash
# Docker 方式（推荐，跨平台）
docker compose -f docker-compose.dev.yml up -d

# 或裸机方式分别启动
cd backend && uvicorn app.main:app --reload --port 3020
cd frontend && npm run dev
```

### 2.4 访问地址

| 服务 | 地址 |
|------|------|
| 前端 | http://localhost:3022 |
| 后端 API 文档 | http://localhost:3020/docs |
| 健康检查 | http://localhost:3020/api/v1/health |
| 默认账号 | admin / admin123 |

### 2.5 环境变量

编辑 `backend/.env`：

| 配置项 | 说明 |
|--------|------|
| DATABASE_URL | PostgreSQL 连接地址 |
| REDIS_HOST / REDIS_PASSWORD | Redis 连接 |
| JWT_SECRET_KEY | JWT 密钥（生产环境必改） |
| FEISHU_APP_ID / FEISHU_APP_SECRET | 飞书应用凭证 |
| FEISHU_REDIRECT_URI | 飞书回调地址 |
| FEISHU_ENABLED | 是否启用飞书登录 |

> 大模型 API Key **不在 .env 配置**：登录管理后台「大模型配置」添加（密钥加密存库，支持多供应商动态切换）。

---

## 三、生产环境部署

### 3.1 架构

5 个 Docker 容器，全部 host 网络模式（PostgreSQL/Redis 仅监听 127.0.0.1，外部访问由防火墙控制）：

| 容器 | 端口 | 说明 |
|------|------|------|
| textmirror-frontend | 3022(HTTPS) / 3080(HTTP→跳转) | Nginx + SSL + 前端 + 反向代理 |
| textmirror-backend | 3020(内部) | FastAPI |
| textmirror-celery | — | Celery Worker（内嵌 beat 定时调度） |
| textmirror-postgres | 127.0.0.1:5432 | postgres:16-alpine，数据持久化到 `./data/postgres` |
| textmirror-redis | 127.0.0.1:6379 | redis:7-alpine，AOF 持久化到 `./data/redis` |

### 3.2 首次部署（服务器端构建，推荐）

```bash
# 1. 克隆代码
cd /opt
git clone <仓库地址> TextMirror
cd /opt/TextMirror

# 2. 配置根目录 .env（数据库/Redis 密码，供 compose 读取，勿提交）
cat > .env <<'EOF'
POSTGRES_PASSWORD=强密码
REDIS_PASSWORD=强密码
EOF

# 3. 配置后端生产环境变量
cp backend/.env.example backend/.env.production
# 编辑 backend/.env.production：数据库/Redis 连接（与上面密码一致）、JWT_SECRET_KEY、大模型 API Key 等

# 4. 创建上传目录并启动（服务器自动构建本机架构镜像）
mkdir -p /opt/UploadFile/textMirrorUpload
chown -R 1000:1000 /opt/UploadFile/textMirrorUpload   # 后端容器以非 root（uid 1000）运行，宿主机挂载目录需一次性授权
docker compose up -d --build
docker compose ps     # 验证状态（应有 5 个容器 running）

# 5. 初始化种子数据（建表已由后端容器启动时自动完成，此步仅首次需要）
docker exec textmirror-backend python -m app.core.seed
```

### 3.3 备选：离线镜像导入（本地构建传 tar）

服务器无法访问外网或不便拉取代码时使用。

**本地操作：**

1. 确认 Docker Desktop 已启动，双击 `build_images.bat`，生成：
   ```
   docker-images\textmirror-backend.tar
   docker-images\textmirror-frontend.tar
   ```

**上传到服务器 `/opt/TextMirror/`（共 5 个文件）：**

| 序号 | 本地路径 | 服务器路径 |
|------|---------|-----------|
| 1 | `docker-compose.yml` | `/opt/TextMirror/docker-compose.yml` |
| 2 | `deploy.sh` | `/opt/TextMirror/deploy.sh` |
| 3 | `backend\.env.production` | `/opt/TextMirror/backend/.env.production` |
| 4 | `docker-images\textmirror-backend.tar` | `/opt/TextMirror/docker-images/textmirror-backend.tar` |
| 5 | `docker-images\textmirror-frontend.tar` | `/opt/TextMirror/docker-images/textmirror-frontend.tar` |

> 服务器目录不存在时手动创建：`mkdir -p /opt/TextMirror/docker-images /opt/TextMirror/backend`；根目录 `.env` 仍需按 3.2 第 2 步配置。

**服务器执行：**

```bash
cd /opt/TextMirror
bash deploy.sh
```

脚本自动完成：创建目录 → 加载镜像 → 启动容器 → 显示运行状态，完成后会询问是否删除 tar 文件（磁盘充足建议保留，便于回滚）。

### 3.4 验证

```bash
curl http://localhost:3020/api/v1/health   # 后端健康检查
curl -k https://localhost:3022/api/v1/health
curl -I http://localhost:3080   # 应 301 跳转 HTTPS
```

浏览器访问：`http://服务器IP:3022`（如配置 HTTPS 则使用 `https://your-domain:3022`）

### 3.5 生产配置文件

| 文件 | 说明 |
|------|------|
| `.env`（项目根目录） | PostgreSQL / Redis 密码（供 compose 读取，勿提交） |
| `backend/.env.production` | 后端生产环境变量（含密钥，勿泄露） |
| `frontend/ssl/` | SSL 证书（可选，启用 HTTPS 时需要） |
| `frontend/nginx.production.conf` | Nginx 生产配置 |
| `docker-compose.yml` | 容器编排 |
| `deploy.sh` | 离线镜像部署脚本（备选模式用） |
| `./data/postgres/`、`./data/redis/` | 数据库 / Redis 持久化数据目录 |

### 3.6 部署完成后的运营建议

首次部署上线后，建议管理员完成以下配置（全部在管理后台实时生效，无需重启）：

1. **修改默认管理员密码**（admin / admin123 仅用于首次登录）
2. **平台品牌定制**（系统设置 → 平台品牌设置）：平台名称、登录页标语、页脚备案号；企业内部部署建议关闭游客模式
3. **配置大模型**（大模型配置）：添加至少一个供应商并设为活跃（种子数据中的示例配置 Key 为占位符，需填入真实 Key 并激活）
4. **审校规则定制**（审校规则）：按业务行业补充自定义规则
5. **初始化词库运营习惯**（全局词库 → 优化建议）：定期查看基于用户反馈的词库优化建议

---

## 四、更新与回滚

### 4.1 日常更新（服务器端构建，推荐）

```bash
cd /opt/TextMirror
git pull
docker compose up -d --build

# 数据库迁移由后端容器入口脚本自动执行（建表 + alembic 版本对齐），确认日志即可
docker logs textmirror-backend 2>&1 | grep -E "数据库迁移已升级|数据库未版本化"
```

> 存量环境首次升级到非 root 镜像（后端以 uid 1000 运行）时，需一次性执行
> `chown -R 1000:1000 /opt/UploadFile/textMirrorUpload`，否则文档上传会因目录无写权限失败。

常见更新场景：

| 场景 | 操作 |
|------|------|
| 前/后端代码有改动 | `git pull && docker compose up -d --build`（compose 只重建有变化的镜像） |
| 只改了 `backend/.env.production` | 上传/修改后 `docker compose restart backend celery-worker` |
| 只改了 `docker-compose.yml` | `docker compose up -d` |

> 只更新后端时前端不受影响，用户无感知。celery worker 已内嵌 beat 定时调度（审计日志 90 天自动清理），重启即生效。

### 4.2 日常更新（离线镜像模式）

本地双击 `build_images.bat` 重新构建，按需上传 tar：

```bash
cd /opt/TextMirror

# 只更新后端
docker load -i docker-images/textmirror-backend.tar
docker compose up -d backend celery-worker
docker logs textmirror-backend 2>&1 | grep -E "数据库迁移已升级|数据库未版本化"

# 只更新前端
docker load -i docker-images/textmirror-frontend.tar
docker compose up -d frontend

# 前后端都更新
bash deploy.sh
```

### 4.3 紧急回滚

**服务器端构建模式：**

```bash
cd /opt/TextMirror
git log --oneline -5              # 找到上一个稳定版本的提交号
git checkout <上一版本提交号>
docker compose up -d --build
# 恢复跟踪最新代码：git checkout <分支名>
```

**离线镜像模式**（回退到上一个 tar 版本）：

1. 将上一次的 `.tar` 镜像文件重新上传（如果服务器上还保留着旧的就不用）
2. 加载旧镜像并重启：
```bash
docker load -i docker-images/textmirror-backend.tar
docker load -i docker-images/textmirror-frontend.tar
docker compose up -d
```

> 建议：每次部署前在服务器保留上一版镜像备份：
> ```bash
> mv docker-images/textmirror-backend.tar docker-images/textmirror-backend.tar.bak
> ```

---

## 五、数据库初始化与迁移

### 5.1 自动迁移（生产，无需手动操作）

后端容器入口脚本在 API 进程启动前自动执行 `python -m app.db_bootstrap`：建表 + Alembic 版本对齐（未版本化的库 stamp 到 head，已版本化的库增量升级）。celery 容器共用镜像但跳过引导，避免并发 DDL 冲突。

```bash
# 确认迁移日志
docker logs textmirror-backend 2>&1 | grep -E "数据库迁移已升级|数据库未版本化"

# 如需手动补跑
docker exec textmirror-backend python -m app.db_bootstrap
```

### 5.2 种子数据

首次部署后执行一次 `python -m app.core.seed`（幂等，已有数据自动跳过），自动创建：

| 类型 | 内容 |
|------|------|
| **角色** | 超级管理员（super_admin）、普通用户（user） |
| **权限** | 完整权限树（菜单 + 按钮级权限，与前端路由一致） |
| **用户** | 管理员 `admin` / `admin123`（生产环境首次登录后立即修改） |
| **全局词库** | 预置常见易错词 |
| **大模型配置** | DeepSeek 示例（Key 为占位符，需在管理后台填入真实 Key 并激活） |

> 旧版 `init_database.py` 已废弃删除：其角色码/权限码与前后端不一致，会导致管理后台入口隐藏、admin 接口 403。统一使用 `app.core.seed`。

### 5.3 表结构变更（开发流程）

本地只负责生成迁移脚本并随代码提交，生产库由容器启动时自动升级（见 5.1）：

```bash
cd backend
venv\Scripts\alembic revision --autogenerate -m "描述"   # 生成迁移脚本（随代码提交）
venv\Scripts\alembic upgrade head                        # 升级本地开发库
```

---

## 六、日常运维

### 6.1 常用命令

```bash
# 查看状态
docker compose ps

# 查看日志
docker compose logs -f                    # 全部
docker compose logs -f backend            # 仅后端
docker compose logs -f celery-worker      # 仅 celery
docker compose logs --tail=100 backend    # 最近100行

# 重启
docker compose restart              # 全部
docker compose restart backend      # 仅后端

# 停止
docker compose down

# 更新代码后重新部署
docker compose up -d --build

# 查看镜像列表
docker images | grep textmirror
```

### 6.2 自动化运维任务

| 任务 | 机制 | 说明 |
|------|------|------|
| 审计日志自动清理 | celery beat 每日触发 | 保留 90 天（与后台手动清理同口径），celery 容器启动即生效，无需配置 |

### 6.3 数据备份

```bash
# 备份上传文件
tar -czf textmirror_uploads_$(date +%Y%m%d).tar.gz /opt/UploadFile/textMirrorUpload/

# 备份数据库（PostgreSQL 运行在容器内）
docker exec textmirror-postgres pg_dump -U textmirror textmirror > textmirror_db_$(date +%Y%m%d).sql

# 数据目录冷备（需先 docker compose down）
tar -czf textmirror_data_$(date +%Y%m%d).tar.gz ./data/
```

### 6.4 SSL 证书续期

替换 `frontend/ssl/` 下的证书文件后：

```bash
docker compose up -d --build frontend
```

### 6.5 磁盘清理

```bash
docker system prune -f   # 清理无用镜像/容器
docker image prune -f    # 仅清理悬空镜像
```

---

## 七、故障排查

### 7.1 容器启动失败

```bash
docker compose logs <容器名>
```

### 7.2 无法访问

```bash
ss -tlnp | grep -E '3022|3080'    # 端口检查
sudo ufw status                     # 防火墙
nslookup your-domain.com            # DNS
```

### 7.3 Nginx 配置问题

```bash
docker compose exec frontend nginx -t
docker compose exec frontend ls -la /etc/nginx/ssl/
```

### 7.4 数据库连接失败

错误信息含 `sqlalchemy.exc.OperationalError: could not connect to server`：

1. 确认 postgres 容器健康：`docker compose ps`（本地裸机开发则确认 PostgreSQL 服务已启动）
2. 检查 `backend/.env.production`（或本地 `.env`）中的数据库连接配置与根目录 `.env` 密码一致
3. 测试连接：
   ```bash
   docker exec -it textmirror-postgres psql -U textmirror -d textmirror
   ```

### 7.5 Redis 连接失败

错误信息含 `redis.exceptions.ConnectionError`：

1. 确认 redis 容器健康：`docker compose ps`
2. 测试连接：
   ```bash
   docker exec -it textmirror-redis redis-cli -a "Redis密码" ping
   # 应返回 PONG
   ```

### 7.6 重新初始化数据库（谨慎，清空所有数据）

```bash
docker exec -it textmirror-postgres psql -U textmirror -d textmirror -c \
  "DROP SCHEMA public CASCADE; CREATE SCHEMA public; GRANT ALL ON SCHEMA public TO textmirror; GRANT ALL ON SCHEMA public TO public;"

docker compose restart backend                          # 重启后自动建表
docker exec textmirror-backend python -m app.core.seed  # 重新写入种子数据
```

### 7.7 忘记管理员密码

推荐走管理后台：另一管理员登录 → 用户管理 → 选中 admin → 重置密码（重置后 admin 的所有登录凭证立即失效，安全）。

无其他可用管理员时，SQL 直写（改后 admin 已登录会话将保持到 Token 自然过期，最长 1 小时）：

```bash
# 生成新密码哈希
docker exec textmirror-backend python -c "
from app.core.security import hash_password
print(hash_password('new_password'))
"

# 写入数据库
docker exec -it textmirror-postgres psql -U textmirror -d textmirror -c \
  "UPDATE users SET password_hash='粘贴上面的哈希值', password_changed_at=now() WHERE employee_id='admin';"
```

### 7.8 大模型配置无法使用

种子数据创建的大模型配置中 API Key 是占位符：登录管理后台 → `大模型配置` → 编辑配置填入真实 API Key → 点击 `激活`。

### 7.9 后端启动失败（本地）

```powershell
# 检查端口占用
Get-NetTCPConnection -LocalPort 3020
# 检查依赖
cd backend && venv\Scripts\pip install -r requirements.txt
```

---

## 八、防火墙

```bash
sudo ufw allow 3022/tcp   # HTTPS
sudo ufw allow 3080/tcp   # HTTP 跳转
```

---

## 九、安全配置

- SSL/TLS 1.2+ 加密，HSTS 安全头
- HTTP 自动 301 跳转 HTTPS
- DEBUG=false（生产环境）
- PostgreSQL / Redis 仅监听 127.0.0.1，Redis 启用 requirepass，不暴露外部端口
- 数据库/Redis 密码存放于根目录 `.env`（不提交仓库）
- Docker 日志大小限制（防磁盘溢出）
- CORS 仅允许指定域名
- Celery 任务软/硬超时保护（5min/6min）
- 生产环境务必修改默认管理员密码，并使用独立数据库用户与强密码

---

## 十、性能配置

| 项目 | 值 |
|------|-----|
| Uvicorn Workers | 2 |
| Celery Workers | 2（solo pool，内嵌 beat） |
| Redis 内存限制 | 512MB（noeviction，AOF 持久化） |
| PostgreSQL max_connections | 200 |
| 上传文件限制 | 50MB |
| Nginx Gzip | 已启用（级别 6） |
| 数据库连接池 | pool_recycle=3600, pool_pre_ping=True |

---

## 十一、文件存储

| 环境 | 路径 |
|------|------|
| 本地开发 | `backend/uploads/` |
| 生产环境 | `/opt/UploadFile/textMirrorUpload/`（Docker volume 挂载） |

包含：用户头像、上传文档、导出文件、平台图标。
