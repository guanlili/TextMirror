# TextMirror 系统运维操作手册

> 适用于当前仓库的开发与生产编排；操作示例不代表已在线上执行或验收。
> 生产命令默认在 `/opt/TextMirror` 仓库根目录执行，路径按实际部署调整；变更前先备份、安排维护窗口。

## 一、环境与架构

| 项目 | 要求与注意事项 |
|------|----------------|
| 生产宿主 | Linux，建议 Ubuntu 22.04+；Docker 与支持 `--wait` 的 Compose 插件 |
| Python | 最低 3.11（使用 `asyncio.timeout`，镜像与 CI 使用 3.11） |
| Node.js | 22，与前端构建镜像一致 |
| 数据服务 | 编排使用 PostgreSQL 16、Redis 7，建议与容器版本保持一致 |
| 本地开发 | macOS / Docker Desktop 使用 `docker-compose.dev.yml` 桥接网络 |
| CPU 架构 | 镜像须匹配目标宿主；Apple Silicon 的 arm64 镜像不能直接用于 amd64 服务器 |

推荐在目标服务器构建。离线构建须显式选择目标平台（如 `linux/amd64`），同时检查应用、PostgreSQL、Redis 镜像架构；出现 `exec format error` 或异常崩溃时先核对架构，不盲目重启。

## 二、本地开发

开发启动概览见 [README](README.md)。Docker 开发先复制并编辑配置，不直接沿用生产地址：

```bash
cp backend/.env.example backend/.env
```

`backend/.env` 至少确认：

```dotenv
DEBUG=true
DATABASE_URL=postgresql+asyncpg://postgres:textmirror@postgres:5432/textmirror
REDIS_HOST=redis
REDIS_PASSWORD=
UPLOAD_DIR=./uploads
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60
```

模板仍写 `JWT_ACCESS_TOKEN_EXPIRE_MINUTES=10080`，复制后须显式改为 `60`，不能依赖代码默认值覆盖环境变量；生产同样如此。

```bash
docker compose -f docker-compose.dev.yml up -d --build --wait postgres redis backend
# 确认 API 引导成功、schema 验证通过，再启动 worker 与前端
docker compose -f docker-compose.dev.yml up -d --build --no-deps celery-worker frontend
```

前端 `http://localhost:3022`，API 文档 `http://localhost:3020/docs`。仅新建 DEBUG 开发库的管理员为 `admin / admin123`，已有账号不会被 seed 重置。
仅 API 源码热加载；前端改动需 `up -d --build frontend`，worker 源码改动须待任务空闲后 `restart celery-worker`（均加开发 compose 参数）。
依赖、Dockerfile 或入口脚本变更需重建镜像；`env_file` 改动须 `up -d --force-recreate backend celery-worker`，`restart` 不会重新注入环境变量。涉及 schema 时按第四节分阶段执行。
裸机运行不会经过 Docker 入口脚本，须先执行 `python -m app.db_bootstrap`、`python -m app.core.seed`，完成 schema 验证后再启动 API/worker；旧库先做第四节预检，不用 `stamp` 替代迁移。

## 三、生产首次部署

### 3.1 默认拓扑与访问边界

生产 `docker-compose.yml` 的五个容器均使用 host 网络，不能用 `postgres`、`redis` 服务名代替回环地址。

| 服务 | 默认监听或职责 |
|------|----------------|
| frontend | Nginx，HTTP `3022`，反向代理 `127.0.0.1:3020` |
| backend | API `3020`，Uvicorn 2 个进程；实际监听 `0.0.0.0`，须限制外部直连 |
| celery-worker | `--pool=solo -B`，单任务串行执行并内嵌 beat，不是并发 2 |
| postgres | `127.0.0.1:5432`，数据在 `./data/postgres` |
| redis | `127.0.0.1:6379`，密码认证、AOF，数据在 `./data/redis` |

默认没有 HTTPS，也没有 `3080` 跳转入口。防火墙只放行所需前端入口与受控管理入口，阻断外部直连 `3020/5432/6379`；公网使用前先配置 TLS。host 网络不提供容器间网络隔离，注意宿主端口冲突。

### 3.2 首次配置

先将仓库克隆到部署目录并检出审核通过的发布版本；以下默认在 `/opt/TextMirror` 执行，不直接部署未合并的功能分支。

1. 在仓库根目录创建受限权限的 `.env`，设置 `POSTGRES_USER`、`POSTGRES_DB`（默认均为 `textmirror`）及独立强密码 `POSTGRES_PASSWORD`、`REDIS_PASSWORD`。
2. 复制模板后逐项编辑，不能直接启动模板配置：

```bash
cp backend/.env.example backend/.env.production
chmod 600 .env backend/.env.production
```

| `backend/.env.production` 必填项 | 要求 |
|--------------------------------|------|
| `DEBUG` | `false`，不要为绕过安全校验改回 true |
| `SECRET_KEY` / `JWT_SECRET_KEY` | 两个独立随机值，各至少 32 字符；可独立执行两次 `openssl rand -hex 32` 生成 |
| `DEFAULT_USER_PASSWORD` | 至少 8 字符强密码，非默认/常见弱密码；用于新用户或管理员重置，不是生产 admin 初始密码 |
| `JWT_ACCESS_TOKEN_EXPIRE_MINUTES` | 显式改为 `60`；模板的 `10080` 会覆盖代码默认值 |
| `DATABASE_URL` | `postgresql+asyncpg://<用户>:<密码>@127.0.0.1:5432/<库名>`，与根目录 `POSTGRES_USER/PASSWORD/DB` 一致 |
| `REDIS_HOST` / `REDIS_PASSWORD` | `127.0.0.1` / 与根目录 Redis 密码一致，端口 `6379`；默认无需 `REDIS_USERNAME` |
| `UPLOAD_DIR` | `/opt/UploadFile/textMirrorUpload`，与 compose 挂载完全一致 |
| `CORS_ORIGINS` | 实际访问源的 JSON 数组，含正确协议、域名和端口，不使用通配放行 |

密码含 URL 特殊字符时须正确编码数据库 URL；数据库/Redis 密码优先使用随机十六进制以减少解析歧义。若保留 `DB_HOST/DB_USER/DB_PASSWORD/DB_NAME`，同步其值，实际连接以 `DATABASE_URL` 为准。
根目录 `.env` 供 Compose 插值；后端和 worker 通过 `env_file` 在容器创建时读取 `backend/.env.production`，不是从镜像自动取得生产配置。
`SECRET_KEY` 用于已保存模型 / 搜索服务密钥等敏感值的解密，须与备份一起安全保管，不可随意更换；轮换需专项迁移或重新配置密钥。用户开放 API 密钥只存哈希，不可解密回原文。`JWT_SECRET_KEY` 变更会使旧登录令牌失效。
模型 API Key 只在管理后台「大模型配置」填写，不写入 env；种子模型的占位 Key 不可用于调用。配置文件、备份和初始密码不得提交或贴入工单。

```bash
sudo mkdir -p /opt/UploadFile/textMirrorUpload
sudo chown -R 1000:1000 /opt/UploadFile/textMirrorUpload
```

后端/worker 以宿主 UID 1000 写入挂载目录；只授权该目录，不用 `chmod 777`。

### 3.3 分阶段启动与验收

以下适用于新空库；旧库必须先完成第四节预检。worker 只依赖数据库/Redis 健康，compose 不会替它等待 API 迁移。

```bash
docker compose build backend frontend
docker compose up -d --wait postgres redis
docker compose up -d --no-deps --wait backend
docker compose logs --tail=100 backend
docker compose exec backend alembic current
docker compose exec backend alembic heads
```

确认引导与 seed 无错误、版本与目标镜像 heads 一致、实际 schema 正确后，再执行：

```bash
docker compose up -d --no-deps celery-worker frontend
docker compose ps
curl -fsS http://127.0.0.1:3020/api/v1/health
curl -fsS http://127.0.0.1:3022/api/v1/health
docker compose exec celery-worker celery -A app.celery_app:celery_app inspect ping
```

生产新建管理员为 `admin`，随机初始密码在首次 seed 日志及容器内 `/app/initial_admin_password.txt`（可由 `INITIAL_ADMIN_PASSWORD_FILE` 改路径）；不是 `admin123`。受控读取后立即登录改密，并安全清理密码文件、限制日志访问；已有 admin 不会重新生成密码。
**公网开放前，在管理后台「系统设置」关闭一键登录并保存**（站点配置 `quick_login_enabled=off`）。默认启用的入口可免密登录管理员，随机密码、改密及 `DEBUG=false` 都不会关闭它；退出后确认按钮消失且直接调用 `POST /api/v1/auth/quick-login?account=admin` 返回 403，不能只检查前端隐藏。
验收还须包括登录、短文本审校、文档上传/异步任务、历史记录、审阅恢复与导出；有授权时再测试事实核查。健康接口成功不等于模型可调用或 worker 可处理任务，不应只看容器 running。

### 3.4 可选 TLS

`frontend/nginx.production.ssl.conf` 是参考配置：启用后才是 HTTPS `3022` 与 HTTP `3080` 跳转。可在外部网关终止 TLS，或经审批单独配置容器 Nginx TLS。
容器方案必须同时完成：选择 SSL 配置、填写域名/证书路径、将证书和私钥安全地只读挂载到 `/etc/nginx/ssl/`、调整前端健康检查、配置防火墙及实际来源/飞书回调。
当前前端 Dockerfile 不复制证书，生产 compose 也不挂载证书；只把证书放进 `frontend/ssl/` 或重建镜像不会自动生效。默认健康检查仅探测 HTTP `80/3022`，TLS 模式须显式改为可用探针（如本机 `3080/health`）。
启用或续期后先 `docker compose exec frontend nginx -t`，再按挂载/部署方案重新加载 Nginx；配置或挂载变更需重建/重建容器。验收证书链、域名、到期日及 `http://域名:3080` 跳转到 `https://域名:3022`，不要用 `curl -k` 掩盖证书问题。

### 3.5 离线镜像流程

在可信构建机按目标架构生成并导出 `textmirror-backend`、`textmirror-frontend` 镜像；worker 共用后端镜像。完全离线还须准备匹配的 PostgreSQL 16、Redis 7 镜像。
安全传输版本匹配的 compose、生产配置和镜像 tar，记录提交号、镜像 ID/摘要、架构与校验和；不要把密钥打入镜像。
服务器用 `docker load -i <镜像文件.tar>` 导入，跳过 build，按首次部署或更新的同一分阶段顺序执行，相关 `up` 命令加 `--no-build --pull never`。保留上一版成套镜像，不直接用一键脚本绕过迁移与任务排空检查。

## 四、更新与数据库 schema

### 4.1 理解自动引导的边界

容器入口仅当首个命令为 `uvicorn` 时，依次执行 `app.db_bootstrap`、`app.core.seed`；worker、单独运行 Alembic 等命令不会执行它们。
bootstrap 先 `Base.metadata.create_all`，再检查 Alembic 版本：有版本执行 `upgrade head`，无版本执行 `stamp head`。seed 幂等补充角色、权限和初始数据，不是旧表结构修复工具。
**`create_all` 不修改已有表，`stamp` 只写版本号，不执行迁移。** 旧库缺列/索引/约束不会因此修好，即使显示 head 也可能存在 schema 漂移。
对无版本旧库、手工改过表或迁移失败的库，必须在启动新 API 前备份并比对实际结构与目标迁移；在恢复副本中演练修复，确认真实基线后再制订迁移方案。不要直接 stamp 到 head 掩盖差异。

本轮功能涉及以下五项迁移，升级包必须包含其脚本及前序依赖：

| 功能 | 迁移文件（位于 `backend/alembic/versions/`） |
|------|-------------------------------------------|
| review 审阅状态 | `d8f2a6b4c901_add_review_state_to_proofread_records.py` |
| quality_feedback 质量反馈/评测 | `e9b3c7d5a102_add_quality_feedback.py` |
| fact_check 事实核查 | `f1c4d8e6a203_add_fact_check.py` |
| search_provider 搜索提供方 | `a2d5e9f7b304_add_fact_check_search_provider.py` |
| llm_usage 用量账本 | `b3e6f8a0c415_add_llm_usage_ledger.py` |

升级前后都记录 `alembic current`；升级后与目标镜像 `alembic heads` 对照，不硬编码“当前 head”。版本一致之外，还要核对新增表、列、约束和实际功能。

### 4.2 更新顺序

1. 选定发布版本，记录当前提交、镜像 ID、配置和数据库版本；保留旧前端/后端（含 worker）匹配镜像。全栈同步更新，不承诺只更新 API 用户无感。
2. 在网关/入口暂停新增业务请求与任务；不要仅凭后台“维护模式”开关就认定已阻断流量。查看队列和任务状态：

```bash
docker compose exec celery-worker celery -A app.celery_app:celery_app inspect active
docker compose exec celery-worker celery -A app.celery_app:celery_app inspect reserved
docker compose exec celery-worker celery -A app.celery_app:celery_app inspect scheduled
```

3. 等待或按业务流程处理 active/reserved/scheduled 及 broker 待消费任务，记录取消/补偿结果；包括事实核查和 webhook。solo 忙碌时 inspect 可能超时，不能把无响应当作空闲；不要强杀、清队列或假定重启会无损重投。
4. 确认任务排空并停止应用写入，再备份数据库、上传文件及配置（第五节）；不要为了缩短等待时间直接执行全栈重启。

```bash
# 完成排空后，停止应用；保留 PostgreSQL/Redis 供备份与迁移使用
docker compose stop celery-worker backend frontend
# 此处完成备份、schema 预检，再更新到审核通过的发布版本
# 拉取代码或导入离线镜像后，在线构建如下
docker compose build backend frontend
docker compose up -d --no-deps --force-recreate --wait backend
docker compose logs --tail=100 backend
docker compose exec backend alembic current
docker compose exec backend alembic heads
# 人工确认迁移与结构验收通过后，才继续
docker compose up -d --no-deps --force-recreate celery-worker frontend
```

5. 重做第三节验收，确认 worker 接收任务与 beat 正常，再恢复流量；迁移失败立即停止发布，不带病启动 worker。
仅改 `backend/.env.production` 也需排空后 `up -d --force-recreate backend celery-worker`，不能 `restart`；如需串行启动，先重建 API 并验收，再重建 worker。compose 配置变更同样用 `up` 重建相关服务。
根目录数据库凭据变更不会自动修改已有 PostgreSQL 数据目录中的用户密码；密码轮换需先设计数据库/Redis 与应用的一致切换，不反复重建或删数据目录。

## 五、备份与回滚

### 5.1 备份与恢复验证

更新前先停止新增请求、排空任务并暂停应用写入，使数据库与上传文件处于同一业务时间点。仅备份代码或镜像不能恢复业务数据。

```bash
umask 077
BACKUP_DIR="/opt/backups/textmirror/$(date +%Y%m%d-%H%M%S)"
mkdir -p "$BACKUP_DIR"
docker compose exec -T postgres sh -c 'pg_dump -U "$POSTGRES_USER" -d "$POSTGRES_DB" -Fc' > "$BACKUP_DIR/database.dump"
tar -czf "$BACKUP_DIR/uploads.tar.gz" -C /opt/UploadFile textMirrorUpload
```

逐条检查退出码、文件大小、归档可读性与校验和。不要直接打包运行中的 `./data/postgres` 作为一致性备份；需要物理备份时使用验证过的 PostgreSQL 备份方法或停库冷备。
另行加密备份根目录 `.env`、`backend/.env.production`、部署/TLS 配置与必要证书，特别保留原 `SECRET_KEY`；访问权限与业务备份分开管理，做异地副本并规定保留期。
Redis 不仅是缓存，也保存配置及任务 broker（DB 8）、结果（DB 9）；按停写窗口采用已验证的 AOF/快照备份，不能用 `FLUSHALL` 或删除数据目录“修复”。恢复旧队列可能重放任务，须单独核对，不能无条件回灌。
定期在隔离环境恢复数据库到新库、解压匹配上传快照并恢复 UID 1000 权限，使用对应镜像和密钥验证登录、历史、附件与模型密钥解密；隔离对外任务投递，避免演练触发真实回调。

### 5.2 回滚决策

- 先保存故障现场并备份当前数据库、上传文件；保留升级前备份、配套旧镜像/配置和迁移记录。
- 回退代码或 `docker load` 旧镜像不等于数据库降级；先证明旧代码兼容当前 schema，再考虑只回滚应用。
- 不兼容时，优先评估修复前进；确需恢复旧库时，在维护窗口恢复成套数据库与上传快照，明确备份后新增数据的损失及补偿方式。
- 不自动执行 `alembic downgrade`。任何逆向迁移都需审查数据损失、在副本演练并经审批；不要用删表、清空 schema 或覆盖数据目录代替恢复方案。
- 回滚也遵守“排空任务 → 停写 → 数据/配置就绪 → API/schema 验证 → worker/前端 → 业务验收”，不要让旧 worker 处理新版本残留任务。

## 六、日常检查与故障处理

### 6.1 日常检查

```bash
docker compose ps
docker compose logs --tail=100 backend celery-worker frontend
docker compose exec frontend nginx -t
docker compose exec redis sh -c 'REDISCLI_AUTH="$REDIS_PASSWORD" redis-cli ping'
df -h
docker system df
```

重点监控健康状态、错误率、任务积压/耗时、磁盘、数据库连接、Redis 内存和备份成功率。Redis 当前上限 512MB、`noeviction`，满额会拒绝写入；先定位占用与容量，不清空队列。
worker 的健康检查不能只依赖 API `/health`，用 `inspect ping`、任务状态与实际消费验证。solo 串行可能被长任务阻塞；不能把配置中的并发数或软/硬超时当作该池的执行保障。扩容前须拆分 beat，避免重复定时调度。
磁盘清理先定位日志、备份与上传占用，再按保留策略定向处理已确认无用的对象；保留回滚镜像，不使用无差别 `docker system prune`，不删除持久卷“腾空间”。

### 6.2 常见故障

| 现象 | 优先处理 |
|------|----------|
| 生产启动拒绝弱密钥/密码 | 核对 `DEBUG=false`、两个独立强密钥及强 `DEFAULT_USER_PASSWORD`，重建 API/worker，不关闭校验 |
| 连接 PostgreSQL/Redis 失败 | 检查健康、host 网络回环地址、两份 env 凭据和实际数据库密码；开发环境改用服务名 |
| `UndefinedColumn`、缺表、迁移异常 | 暂停发布，核对 current/heads、实际 schema 与完整迁移链；备份后在副本修复，禁止用 stamp 掩盖 |
| 页面不可达/502 | 默认先测 HTTP `3022`、API `3020`，查 Nginx、端口冲突和防火墙；未配置 TLS 不测 `3080` |
| 上传拒绝/解析为空 | 查挂载路径与 UID 1000 权限；Nginx `50m` 不等于后端默认 `MAX_UPLOAD_SIZE_MB=20`，以各层限制中更严格者为准；PDF 最多 100 页，无 OCR，扫描件需预处理 |
| 任务长期等待或状态中断 | 查 worker、Redis、队列与关联日志，核对发布/取消记录；不盲目重复提交或清队列，防止重复调用与计费 |
| 模型调用失败/密钥解密失败 | 后台核对激活模型、Key、端点、模型权限与出网；检查 `SECRET_KEY` 是否误换，先恢复匹配密钥而非覆写密文 |
| 改 env 后仍旧值 | `env_file` 只在创建容器时注入；排空后 force-recreate，不是 restart |

### 6.3 忘记管理员密码

优先由另一管理员在「用户管理」重置，并确认「系统设置 → 安全设置」中的重置密码是强密码；通知本人立即改密。
无可用管理员时，须经授权、记录操作并使用受控终端；先备份，隐藏输入生成强密码哈希，避免把明文写入命令行或 shell 历史：

```bash
docker compose exec backend python -c 'from getpass import getpass; from app.core.security import hash_password; print(hash_password(getpass("新密码（至少8字符强密码）: ")))'
docker compose exec postgres sh -c 'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB"'
```

在数据库会话中更新精确账号，哈希仍按敏感信息处理：

```sql
BEGIN;
UPDATE users SET password_hash='<生成的哈希>', password_changed_at=now()
WHERE employee_id='admin' RETURNING id, employee_id;
```

确认仅命中目标一行后单独执行 `COMMIT;`，否则 `ROLLBACK;`。必须同步更新 `password_changed_at`，旧 access/refresh token 随后的鉴权会被拒绝，不是继续有效到自然过期；验证新密码及旧令牌失效，不重新 seed 或改成固定弱密码。

## 七、模型与数据运营

- **模型配置**：后台「大模型配置」维护供应商、模型、端点、Key 与激活状态；用脱敏小样本试跑，核对超时、权限和供应商余额。品牌、游客开关、规则与词库按业务管理，不通过改生产 DEBUG 调整后台选项。
- **飞书入口**：「系统设置 → 飞书对接配置」。当前后台保存到 Redis，而登录链路仍读取 `FEISHU_ENABLED`、`FEISHU_APP_ID`、`FEISHU_APP_SECRET`、`FEISHU_REDIRECT_URI` 进程配置；须在 `backend/.env.production` 配好并重建 API/worker，不能将页面保存视为运行配置已生效。回调与飞书平台登记地址完全一致，端到端验证扫码、回跳和账号关联。
- **审阅与质量反馈**：审阅决定、版本与导出需随历史数据保留；「全局词库 → 质量反馈」先人工确认、脱敏制作独立样例，再发起目标评测。用户忽略建议不自动等于模型错误；评测会调用模型并产生用量，不把反馈未经审核自动加入词库。
- **事实核查**：「系统设置 → 事实核查设置」默认关闭，启用后仍需用户确认并手动启动，审阅内容可能发往外部服务。默认使用当前激活模型原生联网，也可显式配置 Tavily；不支持或失败时不会自动切换 Tavily。
- **信源与费用**：适配器支持不代表具体模型/账号获准联网；可信信源填写纯域名和路径前缀，可信模式需已启用信源。核查结论需人工复核引用；搜索次数限制是尽力执行，不是费用硬上限。
- **用量账本**：后台仪表盘按业务/模型展示实际请求，包括自检、润色、评测、协作与事实核查；重试逐次计数，区分错误、未完成、取消和未知用量。仅记录账本启用后的调用，不回填或叠加旧审校估算；未知不等于零消耗，Token/搜索次数不是费用账单，最终与供应商对账。出现记账失败告警时统计可能不完整。

### 数据保留

- beat 按 Asia/Shanghai 每日 03:30 清理 90 天前审计日志，04:00 回收上传目录；solo 长任务可能延迟执行，须检查实际清理日志而非只看调度配置。
- 上传回收包括已删除记录残留、超过 `ORPHAN_DIR_MIN_AGE_HOURS`（默认 24 小时）的孤儿目录、超过 `GUEST_FILE_RETENTION_DAYS`（默认 7 天）的游客文件。登录用户文件不自动过期，不承诺所有上传均保留 7 天或均永久保留。
- Redis Celery 结果默认保留 24 小时，不等于业务历史保留期；审阅、质量反馈、事实核查、用量账本没有通用自动清理承诺，应按合规要求制定归档/删除策略并验证关联数据与文件一致性。
- 后台「系统设置 → 数据维护」清理前核对范围、备份与审批；审计含全文快照，备份、日志与脱敏样例同样要纳入权限、保留期和到期销毁管理。
