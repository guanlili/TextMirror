[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 120
target-version = "py311"
extend-exclude = ["alembic/versions"]

[tool.ruff.lint]
# 起步只开语法错误与明确缺陷类（E9/F），存量代码零改造即可通过；风格类规则后续再逐步收紧
select = ["E9", "F"]
