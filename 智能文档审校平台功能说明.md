# 智能文档审校平台功能说明

> TextMirror — 企业内部智能文档审校平台，核心能力对标"WPS 黑马校对"

---

## 一、产品定位

支持文本粘贴审校及文档上传审校，在线预览、问题高亮、一键/逐条修改，修改后导出。采用多种在线大模型进行内容校对与建议生成。支持游客免登录体验和飞书扫码登录。

---

## 二、用户端功能

### 2.1 AI 智能润色

- **10种职场润色风格**：正式规范、亲和自然、通俗易懂、极简干练、有理有据、高屋建瓴、落地务实、温和坚定、委婉缓冲、推进行动
- **三段输出**：轻量润色（改动10%-30%）、标准润色（40%-60%）、深度润色（70%-90%）
- 风格卡片选择 → 输入原文（10-5000字）→ AI 生成3个版本 → 一键复制/重新生成/换风格

### 2.2 文本在线校对

- 富文本编辑器输入，支持剪贴板粘贴
- 长文本自动分片校对（~2000字/片），用户无感知
- **审校领域**：通用、公文、法律（内置专业审校规则；特定行业规则由管理员在后台「审校规则」页自定义，实时生效）
- **词库确定性扫描**：敏感词/禁词/纠错词字符串匹配必中（非 AI 概率判断），命中标注〔我的词库〕/〔全局词库〕来源
- **全量智能审校**：错别字、语法、标点、逻辑、表达等 AI 全面检查
- **双栏对照**：左侧原文高亮标注，右侧问题列表（原文→建议 箭头对比）
- **颜色区分**：错字红色、语法黄色、建议蓝色、敏感词橙色、词库命中紫色、放行词绿色虚线
- **悬停联动**：鼠标悬停问题条目，原文对应片段高亮聚焦
- **修改操作**：逐条接受/忽略/撤销，或一键全部修改（带预览确认）
- **导出**：一键复制、导出 TXT/DOCX/PDF

### 2.3 文档上传校对

- **支持格式**：DOCX、PDF（可复制文字）、TXT（≤20MB，PDF ≤100页）
- 上传后保留原文排版（Word 加粗/斜体/字号/字体/颜色/对齐/缩进/表格）
- 双栏对照 + 逐条审改，交互与文本校对一致
- 导出洁净版 DOCX/PDF、审校报告

### 2.4 个性化词库

- 创建多个自定义词库（如"项目A术语库"），支持启用/停用
- 词条维护：单条添加（错误词→正确词）、批量 CSV 导入、搜索/导出
- 校对时多选启用词库，命中时紫色高亮并给出替换建议

### 2.5 放行词管理

- 维护放行词白名单（永久/临时+有效期）
- 校对时命中放行词不标为问题，绿色虚线标记
- 支持批量导入导出

### 2.6 校对历史

- 分页列表，支持类型/领域/时间筛选
- 详情抽屉查看，支持重新编辑和导出
- 覆盖文本校对、文档校对、AI润色三种类型

### 2.7 API 密钥（开放接口）

- 密钥自助创建/吊销（tm_ 前缀、SHA-256 哈希存储、明文仅创建时展示一次）
- 单密钥 RPM + 日配额双层限流，服务端失败自动退还额度
- 端点：文本审校 / 多模型对比 / 异步文档审校（上传→轮询）/ 任务状态 / 可用模型列表
- Swagger 文档常开（/api/v1/open/docs），统一 {code, message} 错误契约

### 2.8 用户账户

- **游客模式**：免登录使用（默认 20次/日/IP），不提供历史和词库；管理员可在品牌设置中**整体关闭**（企业内部部署全员需登录）
- **登录方式**：工号密码登录 / 飞书扫码登录 / 飞书工作台SSO
- **个人中心**：头像、基本信息编辑、密码修改、飞书绑定状态

---

## 三、管理后台功能

| 模块 | 功能 |
|------|------|
| **仪表盘** | 今日校对次数、活跃用户数、调用趋势图、错误类型分布 |
| **用户管理** | 用户列表、创建/编辑/删除、停用/启用、密码重置、批量导入 |
| **角色权限** | RBAC 三级角色，页面级+按钮级权限控制 |
| **大模型配置** | 8种供应商、密钥/参数管理、一键切换活跃模型、连接测试 |
| **全局词库** | 敏感词/禁词/纠错词条/放行词，CRUD + 批量导入；**优化建议看板**（聚合用户接受/忽略反馈，一键采纳为词库） |
| **审校规则** | 各领域审校规则编辑（通用/公文/法律），保存即时生效、内置默认回退 |
| **日志审查** | 全量操作审计（含游客），多维检索；90 天自动定时清理 |
| **系统设置** | 平台品牌（名称/副标题/图标/登录标语/页脚备案/游客模式开关）、飞书对接、全局初始密码、数据维护 |

---

## 四、已实现功能清单

- [x] AI 智能润色（10种风格、三段输出）
- [x] 文本在线校对（分片、高亮、逐条修改、导出）
- [x] 文档上传校对（DOCX/PDF/TXT、保留排版、双栏对照）
- [x] 个性化词库管理（CRUD、批量导入、启停切换）
- [x] 放行词管理（永久/临时、批量导入导出）
- [x] 校对历史记录（含润色历史）
- [x] 全局词库（预置数据 + 优化建议一键采纳）
- [x] 领域专业化校对（通用/公文/法律内置 + 行业规则后台自定义）
- [x] 词库确定性扫描引擎（敏感词/纠错词 100% 召回 + 来源标识）
- [x] 反馈数据飞轮（接受/忽略落库 → 优化建议看板）
- [x] 开放 API（API 密钥 + 5 端点 + Swagger 常开）
- [x] 品牌白标定制（名称/标语/页脚/游客开关，后台实时生效）
- [x] 多大模型管理（8种供应商）
- [x] 飞书对接（扫码登录、工作台SSO、自动建用户、工号获取）
- [x] 个人中心（信息编辑、密码修改、头像、飞书绑定）
- [x] 用户停用/启用、密码重置
- [x] 审计日志系统
- [x] RBAC 权限体系
- [x] Celery 异步任务
- [x] Docker 生产部署（SSL + Nginx + 3容器架构）
- [x] 全站移动端适配